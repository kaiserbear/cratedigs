routes:
  /artists-directory/:
    template: artists
    content_type: application/json

collections:
  /:
    permalink: /{slug}/
    template: index

taxonomies:
  tag: /tag/{slug}/
  author: /author/{slug}/


